
.btn
    display flex
    align-items center
    justify-content center
    padding 16px 40px
    border-radius 8px
    max-height 46px
    cursor pointer

.btn--primary
    background-color: $brand-800
    color: $white
    font-size 1.4rem
    font-weight 500
    letter-spacing .03em
    font-en()
